package turtle.util;

public enum Rotation {
    /**
     * represents directions in which a turtle can be rotated.
     * can be either left (anticlockwise) or right (clockwise).
     */
    LEFT,
    RIGHT;

    public static final int ROTATIONAL_UNIT = 45;
}
